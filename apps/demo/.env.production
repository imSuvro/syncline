# The deployed demo talks to the deployed sync server. Vite reads this
# during `vite build`; `pnpm dev` falls back to http://localhost:8787.
VITE_SYNC_URL=https://syncline-server.weekendbuild.workers.dev
