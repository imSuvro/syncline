{
  "name": "syncline-server-cf",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare adapter: Worker router + WorkspaceDO + DirectoryDO on DO SQLite with WebSocket Hibernation",
  "type": "module",
  "dependencies": {
    "@syncline/protocol": "workspace:*",
    "@syncline/server": "workspace:*",
    "syncline-demo-schema": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250830.0",
    "wrangler": "^4.40.0"
  }
}
