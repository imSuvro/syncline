// @syncline/client — sans-IO sync core (src/core, ADR-001) plus thin
// browser adapters (src/adapters: IndexedDB storage, WebSocket transport,
// runtime). Stage-10 (backlog D1–D6) implements the engine; this
// placeholder keeps the workspace compiling from stage 7.
export const CLIENT_VERSION = '0.1.0';
