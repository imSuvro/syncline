// @syncline/protocol — everything both sides must agree on, as pure data
// and pure functions: frame types + codecs (ADR-002), the permission
// ruleset + evaluator (ADR-003), and per-field LWW merge (ADR-005).
// Stage-8 (backlog B1–B3) implements the full surface; this placeholder
// keeps the workspace compiling and the pack-check meaningful from stage 7.
export const PROTOCOL_VERSION = 1;
